javascript:(function () {      function getOriginCoords(i, row) {         var villName = $(row).children('td').eq(2).text();         return villName.match(/\d{3}\|\d{3}/)[0];     }      function count(coords) {         return coords.reduce(function (acc, coord) {             if (acc[coord] == undefined) {                 acc[coord] = 1;             } else {                 acc[coord]++;             }             return acc;         }, {});     }      var $rows = $('#incomings_table tbody').children('.row_a, .row_b');     var coords = $rows.map(getOriginCoords).toArray();     var attackCount = count(coords);      $rows.each(function (i, row) {         var coords = getOriginCoords(i, row);         var count = attackCount[coords];          $(row).children('td').eq(0).prepend("(" + count + " Commands)");         if (count <= 3)  {             $(row).children('td').css({ "background-color" : "#FF69B4" });         }       if (count == 4)  {             $(row).children('td').css({ "background-color" : "#FF8C00" });         }         if (count == 5)  {             $(row).children('td').css({ "background-color" : "#FF8C00" });         }         if (count >= 6)  {             $(row).children('td').css({ "background-color" : "#FFD700" });         }         if (count >= 20)  {             $(row).children('td').css({ "background-color" : "#20a90b" });         }            });  })();

